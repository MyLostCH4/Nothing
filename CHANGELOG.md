# 更新日志

## 1.5.1 - 2026-08-28

### 修复

- 体重图不再把没有记录的日期按 0 kg 绘制，消除了从零点跳向真实体重的虚假曲线。
- 体重曲线现在直接连接真实测量点，不再使用账户余额式的逐日延续逻辑。

### 改进

- 体重纵轴根据当前显示范围内的有效记录自动缩放，不再强制包含零点。
- 纵轴加入约 15% 的显示边距、2 kg 最小跨度和易读刻度，兼顾细微趋势与视觉真实性。
- 修正 Windows“已安装的应用”中遗留的旧版本号。

### 兼容性

- 数据库结构和 `.nothingdata` 格式均未变化，可直接继承 1.5.0 及更早版本的数据。
- 安装升级只替换程序文件，不覆盖现有 SQLite 数据。

## 1.5.0 - 2026-08-19

### 新增

- work 的理论、试验和小组工作记录支持双击打开独立详情窗口。
- 详情窗口支持长文本编辑、鼠标滚轮和滚动条浏览。
- 文本修改后每 1 秒自动保存，关闭窗口前会执行最后一次保存。
- 支持同时打开多条不同的 work 记录；同一条记录重复打开时会定位到已有窗口。

### 改进

- work 详情窗口使用 Windows 原生标题栏的最小化、最大化和关闭按钮。
- 概览页的理论、试验和小组工作区域严格三等分，不再受内容长度影响。
- 编辑保存后同步刷新 work 表格与概览摘要。

### 兼容性

- 数据库结构没有变化，可直接读取 1.4 及更早版本的 SQLite 数据和 `.nothingdata` 备份。
- 安装升级不会覆盖现有个人数据。
